#include<stdio.h>

/**
 * main - entry point
 * @argc: argument counters
 * @argv: array of arguments from cmd
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
