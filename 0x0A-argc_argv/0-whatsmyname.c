#include<stdio.h>

/**
 * main - runs the code
 * @argc: argument counters
 * @argv: array of arguments from cmd
 * Return 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return 0;
}
